import SettingsClient from "@/components/settings-client";
export const dynamic = "force-dynamic";
export const metadata = { title: "Reglas de pádel" };
export default function SettingsPage() {
  return <SettingsClient />;
}
